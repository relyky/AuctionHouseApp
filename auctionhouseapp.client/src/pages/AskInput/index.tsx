import { Container, Typography } from "@mui/material";

export default function AskInput() {

  return (
    <Container>
      <Typography variant='h5'>Open Ask 現場輸入</Typography>

    </Container>
  )
}